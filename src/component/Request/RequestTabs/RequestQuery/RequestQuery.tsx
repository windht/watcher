import React from "react";
import { RequestParamsList } from "../RequestParamsList";

type Props = {};

export const RequestQuery = (props: Props) => {
  return <RequestParamsList fieldName="params" />;
};
